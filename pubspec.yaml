name: mikhail_samuel
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_launcher_icons: ^0.14.3
  flutter_svg_provider: ^1.0.7

  # تأكد إذا دي فعلاً مكتبة داخلية أو تم إضافتها بالخطأ
  flutter_application_1: ^0.0.2
  intl_utils: ^2.8.10
  camera: ^0.11.1
  tflite: ^1.1.2


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/labels.txt
    - assets/model.tflite

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/main1.png"

flutter_intl:
  enabled: true
